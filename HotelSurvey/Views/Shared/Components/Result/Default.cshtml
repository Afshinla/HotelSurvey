@model IEnumerable<HotelSurvey.Models.Result>

@{
    ViewData["Title"] = "Default";
}

@*<h2>Default</h2>*@

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>
                @*<th>
                    @Html.DisplayNameFor(model => model.ResultText)
                </th>*@
                @*<th>
                    @Html.DisplayNameFor(model => model.ResultNummer)
                </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ResultText)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResultNummer)
            </td>
            <td>
                <a asp-controller="Result" asp-action="ListResponses" @*asp-route-id="@item.ResultId"*@>Add</a>
                @*<a asp-action="Edit" asp-route-id="@item.ResultId">Edit</a> |
        <a asp-action="Details" asp-route-id="@item.ResultId">Details</a> |
        <a asp-action="Delete" asp-route-id="@item.ResultId">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
